spring.application.name=backend

server.port=8080

# MySQL (Configured for Docker + Local fallback)
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:hkayet8ram}?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:appuser}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:AppSecret123!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
jwt.secret=${JWT_SECRET:hkayet8ram_super_secret_key_change_this_in_production}
jwt.expiration=86400000

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:didf8ien7}
cloudinary.api-key=${CLOUDINARY_API_KEY:694159576365642}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:NrS8DEUk8WT47h0hSTe5ER18oQw}

# Upload max size
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}

#cors.allowed-origins=http://127.0.0.1:44015

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true