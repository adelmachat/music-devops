apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-music-app.fullname" . }}
  labels:
    {{- include "helm-music-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-music-app.selectorLabels" . | nindent 4 }}
