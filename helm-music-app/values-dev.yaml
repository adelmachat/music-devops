namespace: music-helm-dev

backend:
  replicaCount: 1
  image:
    repository: music-backend
    tag: "1.1"
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 8080
    targetPort: 8080
  config:
    DB_HOST: mysql-service
    DB_PORT: "3306"
    DB_NAME: hkayet8ram

frontend:
  replicaCount: 1
  database: hkayet8ram
  image:
    repository: music-frontend
    tag: "1.1"
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 80
    targetPort: 80

mysql:
  replicaCount: 1
  database: hkayet8ram
  image:
    repository: mysql
    tag: "8.0"
    pullPolicy: IfNotPresent
  service:
    port: 3306
    targetPort: 3306
  storage: 2Gi
  secret:
    username: appuser
    password: AppSecret123!
    rootPassword: RootSecret123!

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: music-helm.local
      paths:
        - path: /api
          pathType: Prefix
        - path: /
          pathType: Prefix
  tls: []